// Custom offline font variable mappings to bypass remote Google Fonts downloading in sandboxed environments.
// Fallback font families and Google Font imports are defined inside globals.css.

export const cormorant = {
  variable: 'font-cormorant',
};

export const dmSans = {
  variable: 'font-dm-sans',
};

export const jetbrainsMono = {
  variable: 'font-jetbrains',
};

export const hind = {
  variable: 'font-hind',
};
